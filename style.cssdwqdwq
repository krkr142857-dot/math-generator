/* 기본 초기화 */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
    --bg: #f5f6fa; --w: #fff; --bd: #e5e7eb; --bd2: #f0f1f5;
    --tx: #111827; --sub: #6b7280; --dim: #9ca3af;
    --ac: #4f46e5; --ac-lt: #ede9fe; --ac-md: #818cf8;
    --gn: #16a34a; --gn-bg: #f0fdf4; --gn-bd: #86efac;
    --rd: #dc2626; --rd-bg: #fef2f2; --rd-bd: #fca5a5;
    --yl: #d97706; --yl-bg: #fffbeb; --yl-bd: #fcd34d;
    --sh: 0 1px 3px rgba(0,0,0,.07); --sh2: 0 4px 14px rgba(0,0,0,.09);
}

[data-theme="dark"] {
    --bg: #0f1117; --w: #1a1d27; --bd: #2d3148; --bd2: #232636;
    --tx: #e2e8f0; --sub: #94a3b8; --dim: #64748b;
    --ac: #818cf8; --ac-lt: #1e1b4b; --ac-md: #6366f1;
    --gn: #4ade80; --gn-bg: #052e16; --gn-bd: #166534;
    --rd: #f87171; --rd-bg: #450a0a; --rd-bd: #991b1b;
    --yl: #fbbf24; --yl-bg: #1c1002; --yl-bd: #92400e;
}

body { background: var(--bg); font-family: 'Noto Sans KR', sans-serif; color: var(--tx); height: 100vh; overflow: hidden; display: flex; flex-direction: column; }

/* SPLASH SCREEN */
#splash { position: fixed; inset: 0; z-index: 999; background: var(--bg); display: flex; align-items: center; justify-content: center; }
.sp-card { background: linear-gradient(145deg, #1e1b4b 0%, #312e81 45%, #4338ca 100%); border-radius: 28px; padding: 52px 60px 44px; display: flex; flex-direction: column; align-items: center; box-shadow: 0 24px 64px rgba(79,70,229,.22); max-width: 480px; width: 88%; position: relative; overflow: hidden; }
.sp-icon { width: 72px; height: 72px; background: rgba(255,255,255,.15); border: 2px solid rgba(255,255,255,.3); border-radius: 18px; display: flex; align-items: center; justify-content: center; font-size: 34px; font-weight: 900; color: #fff; margin-bottom: 22px; }
.sp-title { font-size: 32px; font-weight: 800; color: #fff; letter-spacing: -1px; margin-bottom: 8px; text-align: center; }
.sp-sub { font-size: 13.5px; color: rgba(255,255,255,.6); margin-bottom: 20px; }
.sp-badge-lg { display: inline-block; width: 180px; text-align: center; padding: 6px 0; border-radius: 10px; font-size: 12.5px; font-weight: 700; background: rgba(255,255,255,.15); border: 1.5px solid rgba(255,255,255,.3); color: #fff; margin-bottom: 8px; }
.sp-start { padding: 14px 46px; border-radius: 12px; border: none; background: #fff; color: #4338ca; font-size: 16px; font-weight: 800; cursor: pointer; transition: all .2s; margin-top: 22px; }
#splash.hidden { display: none; }

/* HEADER */
header { flex-shrink: 0; background: var(--w); border-bottom: 1px solid var(--bd); padding: 0 20px; height: 50px; display: flex; align-items: center; justify-content: space-between; }
.logo { display: flex; align-items: center; gap: 10px; }
.logo-ico { width: 30px; height: 30px; background: var(--ac); border-radius: 7px; display: flex; align-items: center; justify-content: center; font-size: 15px; font-weight: 900; color: #fff; }
.logo-tx { font-weight: 800; font-size: 14px; }
.theme-btn { border: 1.5px solid var(--bd); background: var(--bg); cursor: pointer; width: 34px; height: 34px; border-radius: 10px; display: flex; align-items: center; justify-content: center; }

/* LAYOUT */
.ws { flex: 1; display: flex; overflow: hidden; }
aside { width: 224px; flex-shrink: 0; background: var(--w); border-right: 1px solid var(--bd); padding: 10px 11px; display: flex; flex-direction: column; overflow-y: auto; gap: 8px; }
.cp { flex: 1; overflow-y: auto; padding: 16px; display: flex; flex-direction: column; gap: 12px; }
.rp, .kp { width: 260px; flex-shrink: 0; background: var(--w); border-left: 1px solid var(--bd); overflow-y: auto; padding: 16px 14px; }

/* UI COMPONENTS */
.grade-tabs { display: flex; gap: 4px; }
.grade-tab { flex: 1; padding: 7px 3px; border-radius: 7px; border: 1.5px solid var(--bd); background: transparent; cursor: pointer; font-size: 11.5px; font-weight: 600; }
.grade-tab.on { background: var(--ac); color: #fff; border-color: var(--ac); }

.sub-list { display: flex; flex-direction: column; gap: 2px; }
.sub-btn { padding: 8px 10px; border-radius: 8px; border: 1.5px solid var(--bd); background: transparent; text-align: left; cursor: pointer; font-size: 13px; }
.sub-btn.on { border-color: var(--ac); background: var(--ac-lt); color: var(--ac); font-weight: 700; }
.sub-btn .sd { font-size: 10px; color: var(--dim); margin-top: 2px; font-weight: 400; }

.lv-row { display: flex; gap: 4px; }
.lv-btn { flex: 1; height: 32px; border-radius: 6px; border: 1.5px solid var(--bd); background: transparent; cursor: pointer; }
.lv-btn.on { font-weight: 700; }

#genBtn { width: 100%; padding: 12px; border-radius: 10px; border: none; background: var(--ac); color: #fff; font-weight: 700; cursor: pointer; margin-top: auto; }

/* 수식 키보드 */
.mk-grid { display: grid; grid-template-columns: repeat(10, 1fr); gap: 4px; }
.mkb { padding: 8px 2px; border-radius: 6px; border: 1.5px solid var(--bd); background: var(--w); font-size: 11px; cursor: pointer; }
.kb-area { height: 0; overflow: hidden; transition: height 0.3s; }
.kb-area.open { height: auto; padding-top: 10px; }

/* 문제 카드 스타일 등 추가... (지면상 핵심만 로드) */
